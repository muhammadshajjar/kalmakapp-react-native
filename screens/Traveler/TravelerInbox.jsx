import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const TravelerInbox = () => {
  return (
    <SafeAreaView>
      <Text>TravelerInbox</Text>
    </SafeAreaView>
  );
};

export default TravelerInbox;
